/** @type {import('next').NextConfig} */
const nextConfig = {
  // Serve everything in /public at the root — no sub-path needed
  // since each app is its own Vercel project with its own domain.
};

module.exports = nextConfig;
